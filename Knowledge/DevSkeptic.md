# Dev Skeptic Knowledge

This file captures reusable dev-centric context for the DevSkeptic agent. Populate it with:

- **App patterns** — common request flows, auth models, caching patterns used in this org
- **Common mitigations** — known controls already in place (e.g., "all SQL access via parameterised queries enforced by shared ORM")
- **Org conventions** — coding standards, deployment patterns, review gates

## Org-Specific Context

> **Not yet populated.** Fill this in for your organisation to improve DevSkeptic review accuracy.

## Framework / Stack Notes

> Add notes about the tech stack here (e.g., "All services use Flask + SQLAlchemy; secrets always fetched from Key Vault at startup").

## Known False-Positive Patterns

> Document patterns that routinely trigger findings but are already mitigated at the org level.
