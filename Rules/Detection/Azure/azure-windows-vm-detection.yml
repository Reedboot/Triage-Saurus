rules:
  - id: context-azure-windows-vm
    message: |
      Azure Windows Virtual Machine detected in infrastructure.
    severity: INFO
    languages: [terraform, hcl]
    pattern-either:
      - pattern: |
          resource "azurerm_windows_virtual_machine" $NAME {
            ...
            network_interface_ids = $NIC_IDS
            ...
          }
      - pattern: |
          resource "azurerm_windows_virtual_machine" $NAME {
            ...
          }
      - patterns:
          - pattern: |
              module $MOD {
                ...
                source = $SRC
                ...
              }
          - metavariable-regex:
              metavariable: $SRC
              regex: "(?i).*windows[_-]?vm.*"
    metadata:
      finding_kind: Asset
      asset_provider: azure
      rule_type: context_discovery
      category: infrastructure
      subcategory: compute
      technology: [terraform, azure, compute]
      target_files: ["*.tf", "**/*.tf"]
      extracts:
        service_type: Windows Virtual Machine
        resource_type: azurerm_windows_virtual_machine
        resource_name: $NAME
        properties:
          - name: network_interface_ids
            pattern: network_interface_ids = $NIC_IDS
          - name: size
            pattern: size = "$SIZE"
          - name: admin_username
            pattern: admin_username = "$ADMIN"
    description: |
      Detects Azure Windows Virtual Machine resources and captures network interface references for parent-child linking.
