rules:
  - id: context-azure-postgresql-database
    message: |
      Azure PostgreSQL Database detected in infrastructure.
    severity: INFO
    languages: [terraform, hcl]
    pattern-either:
      - pattern: |
          resource "azurerm_postgresql_database" $NAME {
            ...
            server_name = $SERVER_NAME
            ...
          }
      - patterns:
          - pattern: |
              module $MOD {
                ...
                source = $SRC
                ...
              }
          - metavariable-regex:
              metavariable: $SRC
              regex: "(?i).*postgresql[_-]?database.*"
    metadata:
      finding_kind: Asset
      asset_provider: azure
      rule_type: context_discovery
      category: infrastructure
      subcategory: postgresql
      technology: [terraform, azure, postgresql]
      target_files: ["*.tf", "**/*.tf"]
      extracts:
        service_type: PostgreSQL Database
        resource_type: azurerm_postgresql_database
        resource_name: $NAME
        properties:
          - name: server_name
            pattern: server_name = $SERVER_NAME
          - name: charset
            pattern: charset = "$VAL"
          - name: collation
            pattern: collation = "$VAL"
    description: |
      Detects Azure PostgreSQL Database resources and captures the parent PostgreSQL server reference via server_name.
