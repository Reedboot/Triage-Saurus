rules:
  - id: diagram-gap-aws-queue-detection
    message: |
      Diagram coverage gap detected for 'queue' in repo 'AWSGoat'.
      Resource appears in repo evidence but diagram is missing a required connection or representation.
    severity: WARNING
    languages: [regex]
    patterns:
      - pattern-regex: '(?i)queue'
    metadata:
      category: security
      subcategory: [asset-discovery-gap]
      technology: [aws]
      finding_kind: asset_detection
      generated_by: web_parallel_scan_validator
