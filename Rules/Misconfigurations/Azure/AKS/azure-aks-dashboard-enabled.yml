rules:
  - id: azure-aks-dashboard-enabled
    message: |
      AKS cluster has the legacy Kubernetes dashboard explicitly enabled.
      The dashboard exposes cluster resources via an unauthenticated UI and has a history of CVEs.
      Remove the kube_dashboard block or set enabled = false.
      ATTACK: Browse to Kubernetes dashboard URL → unauthenticated access → enumerate all cluster resources → deploy backdoor privileged pod
      TOOLS: kubectl proxy, Browser, Peirates, kube-hunter, AzureHound
      CHAIN: public dashboard URL accessible → unauthenticated dashboard access → cluster resource enumeration → malicious pod deployment → RCE
      IMPACT: Full cluster compromise via unauthenticated dashboard; privileged pod deployment enables node escape and lateral movement
    severity: ERROR
    languages: [terraform, hcl]
    pattern-either:
      - pattern: |
          resource "azurerm_kubernetes_cluster" "$NAME" {
            ...
            addon_profile {
              ...
              kube_dashboard {
                enabled = true
              }
              ...
            }
            ...
          }
      - pattern: |
          resource azurerm_kubernetes_cluster "$NAME" {
            ...
            addon_profile {
              ...
              kube_dashboard {
                enabled = true
              }
              ...
            }
            ...
          }
    metadata:
      category: security
      subcategory: [exposure, cloud, kubernetes]
      cwe: CWE-668
      confidence: HIGH
      likelihood: MEDIUM
      impact: HIGH
      technology: [terraform, azure, kubernetes]
      five_pillars: Pillar 1 (Network Security)
