name: OpenGrep Security Scan

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install opengrep
        run: |
          sudo curl -sSL https://github.com/opengrep/opengrep/releases/latest/download/opengrep_manylinux_x86 -o /usr/local/bin/opengrep
          sudo chmod +x /usr/local/bin/opengrep

      - name: Verify opengrep installation
        run: opengrep --version

      - name: Set up mock environment and expanded corpus
        run: |
          # Prepare TestsCorpus and ensure terragoat is available as a git-tracked repo
          REPOS_ROOT="$(cd "$GITHUB_WORKSPACE/.." && pwd)"
          CORPUS_DIR="$GITHUB_WORKSPACE/TestsCorpus"
          mkdir -p "$CORPUS_DIR"

          # Prefer cloned terragoat in TestsCorpus (clone if missing)
          if [ ! -d "$CORPUS_DIR/terragoat/.git" ]; then
            echo "[ci] Cloning terragoat into TestsCorpus"
            git clone --depth 1 https://github.com/bridgecrewio/terragoat "$CORPUS_DIR/terragoat"
          else
            echo "[ci] terragoat already present in TestsCorpus"
          fi

          # Place terragoat where run_cozo_repos.sh expects it (parent/terragoat)
          cp -r "$CORPUS_DIR/terragoat" "$REPOS_ROOT/terragoat" || true

          # Tell the script which repos to scan (use terragoat)
          echo "terragoat" > "$GITHUB_WORKSPACE/Intake/ReposToScan.txt"

          # Ensure the Learning output directory exists so the DB can be written.
          mkdir -p "$GITHUB_WORKSPACE/Output/Learning"

      - name: Validate Opengrep rules and sample scan against expanded corpus
        working-directory: ${{ github.workspace }}
        run: |
          OUTPUT_FILE="opengrep_output.json"

          # Run opengrep against the expanded corpus to exercise more rules
          opengrep scan --config Rules/ --json --output "$OUTPUT_FILE" "$GITHUB_WORKSPACE/TestsCorpus" || true

          if [ ! -f "$OUTPUT_FILE" ]; then
            echo "❌ opengrep did not produce an output file: $OUTPUT_FILE"
            exit 1
          fi

          results_count=$(jq '.results | length' opengrep_output.json)
          errors_count=$(jq '.errors | length' opengrep_output.json)
          echo "opengrep findings: ${results_count}, errors: ${errors_count}"
          if [ "${errors_count}" -gt 0 ]; then
            echo "❌ opengrep reported rule errors:"
            jq '.errors' opengrep_output.json || true
            exit 1
          fi
          if [ "${results_count}" -eq 0 ]; then
            echo "❌ opengrep produced zero findings for the corpus; aborting"
            exit 1
          fi
          echo "✅ opengrep rules validated and sample scan produced results"

      - name: Upload opengrep output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opengrep-output
          path: opengrep_output.json
          if-no-files-found: warn



  init-test:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install opengrep
        run: |
          sudo curl -sSL https://github.com/opengrep/opengrep/releases/latest/download/opengrep_manylinux_x86 -o /usr/local/bin/opengrep
          sudo chmod +x /usr/local/bin/opengrep

      - name: Verify opengrep installation
        run: opengrep --version

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run init_cozo_learning test
        run: bash Scripts/Tests/test_run_cozo_init.sh
        shell: bash

